# Local .terraform directories
**/.terraform/*

# .tfstate files (These contain raw passwords, NEVER commit them)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Ignore any .tfvars files that contain secret passwords
*secret*.tfvars

# Ignore AWS CLI credentials if accidentally copied here
.aws/
